<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChatX" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/emerald</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/yellow</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/emerald</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Here you can add color of your choice  -->
    <color name="ucrop_color_toolbar">@color/emerald</color>
    <color name="ucrop_color_statusbar">@color/emerald</color>
    <color name="ucrop_color_widget_active">@color/emerald</color>
</resources>
